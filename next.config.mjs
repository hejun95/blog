/** @type {import('next').NextConfig} */

let assetPrefix = "/blog/";
let basePath = "/blog/";

const nextConfig = {
  // basePath,
  // assetPrefix,
  output: "export",
};

export default nextConfig;

